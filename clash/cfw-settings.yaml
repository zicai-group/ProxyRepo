profileParsersText: >-
  parsers: # array
    # - reg: ^.*$ 匹配所有订阅，或  - url: https://example.com/profile.yaml 指定订阅
    - url: https://sub.websubscribe.net/api/v1/client/subscribe
    # 删除服务商提供的策略组和规则
      code: |
        module.exports.parse = (raw, { yaml }) => {
          const rawObj = yaml.parse(raw)
          const groups = []
          const rules = []
          return yaml.stringify({ ...rawObj, 'proxy-groups': groups, rules })
        }
      file: "E:/Clash/data/parsers/parser-Loyalsoldier.yaml"
      # file: "E:/Clash/data/parsers/parser-Hackl0us.yaml"

